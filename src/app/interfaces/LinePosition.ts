export interface LinePosition {
    id?: number;
    name: string;
    email: string;
    phone: string;
    peopleNumber: number;
    priority: string;
    description?: string;
    waitingTime: number;
}
